-define(CPUDUTYUSED, cpuDutyUsed).      %% cpu usage
-define(MEMORYFREE, memoryFree).        %% The amount of free memory
-define(MEMORYSIZE, memorySize).        %% Total Memory
-define(MEMORYUSED, memoryUsed).        %% Memory usage


-define(DISKTOTAL,disktotal).               %% total disk 
-define(DISKFREE,diskfree).                 %% free
-define(DISKUSED,diskused).
-define(DISKPERCENTUSED,diskpercentUsed).      %% disk used rate
-define(DISKDESC,diskdesc).      %% 


%~ -define(MEMORYUSED, memoryUsed).        %% 
-define(MEMORYUNTIS, memoryUntis).        %% memory until


-define(PROCESSNAME, processName).      %% process name
-define(PROCESSSTATUS, processStatus).  %% process status
-define(CPUUSED, cpuUsed).              %% process cpuUsed
